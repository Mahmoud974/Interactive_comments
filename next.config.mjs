/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["zhqpheunskiuodymniyz.supabase.co", "source.unsplash.com"], // Ajoute ici le domaine que tu veux autoriser
  },
};

export default nextConfig;
